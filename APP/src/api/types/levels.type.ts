export interface Getlevel {
    id: number;
    name: string;
    points: number;
    description: string;
    createdAt: string;
    updatedAt: string;
}

export interface Postlevel {
    name: string;
    points: number;
    description: string;
}